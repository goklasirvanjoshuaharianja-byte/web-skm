<!DOCTYPE html>
<html>
<head>
  <title><%= member.name %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-10">

<div class="bg-white shadow-xl rounded-2xl max-w-xl w-full p-8 text-center">

  <img src="/uploads/<%= member.photo %>"
       class="w-36 h-36 mx-auto rounded-full object-cover mb-6">

  <h1 class="text-2xl font-bold text-emerald-800 mb-2">
    <%= member.name %>
  </h1>

  <p class="text-emerald-600 mb-4">
    <%= member.position %>
  </p>

  <p class="text-gray-600 mb-6 leading-relaxed">
    <%= member.bio %>
  </p>

  <div class="flex justify-center space-x-6">
    <% if(member.instagram){ %>
      <a href="<%= member.instagram %>" class="text-pink-500">Instagram</a>
    <% } %>

    <% if(member.linkedin){ %>
      <a href="<%= member.linkedin %>" class="text-blue-600">LinkedIn</a>
    <% } %>
  </div>

  <div class="mt-6">
    <a href="/anggota" class="text-gray-500 hover:underline">
      ← Kembali
    </a>
  </div>

</div>

</body>
</html>
