<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Sumber Kemakmuran</title>

<script src="https://cdn.tailwindcss.com"></script>

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet">

<link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

<style>
  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Inter', sans-serif;
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  body.page-loaded {
    opacity: 1;
  }

  body.page-exit {
    opacity: 0;
  }

  h1, h2, h3 {
    font-family: 'Poppins', sans-serif;
  }

  /* Floating animation */
  @keyframes float {
    0%,100% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
  }

  .floating {
    animation: float 6s ease-in-out infinite;
  }

  /* Elegant glow */
  .glow:hover {
    box-shadow: 0 0 30px rgba(16,185,129,0.5);
  }

  /* Logo reveal animation */
  @keyframes logoReveal {
    0% {
      opacity: 0;
      transform: translateY(-25px) scale(0.9);
    }
    60% {
      opacity: 1;
      transform: translateY(5px) scale(1.05);
    }
    100% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .logo-reveal {
    animation: logoReveal 1.2s ease-out forwards;
  }

  /* Navbar menu reveal */
  .menu-item {
    opacity: 0;
    transform: translateY(-10px);
    animation: fadeMenu 0.8s ease forwards;
  }

  .menu-item:nth-child(1) { animation-delay: 0.6s; }
  .menu-item:nth-child(2) { animation-delay: 0.8s; }
  .menu-item:nth-child(3) { animation-delay: 1s; }
  .menu-item:nth-child(4) { animation-delay: 1.2s; }

  @keyframes fadeMenu {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

</head>
<body class="bg-gray-50">

<!-- NAVBAR -->
<nav class="fixed w-full z-50 backdrop-blur-lg bg-white/10 border-b border-white/20">
  <div class="container mx-auto flex justify-between items-center px-6 py-4 text-white">

    <!-- Logo -->
    <div class="logo-reveal">
      <img src="/images/logo.png"
           alt="Logo SKM"
           class="h-11 w-auto object-contain drop-shadow-lg transition duration-300 hover:scale-105">
    </div>

    <!-- Menu -->
    <div class="space-x-8 text-sm font-medium tracking-wide">
      <a href="#home" class="hover:text-lime-300 transition menu-item">Home</a>
      <a href="#about" class="hover:text-lime-300 transition menu-item">Tentang</a>
      <a href="/anggota" class="hover:text-lime-300 transition menu-item">Anggota</a>
      <a href="/gallery" class="hover:text-lime-300 transition menu-item">Gallery</a>
    </div>

  </div>
</nav>

<!-- HERO -->
<section id="home"
class="h-screen flex items-center justify-center text-center text-white
bg-gradient-to-br from-emerald-900 via-teal-700 to-green-400 relative overflow-hidden">

  <div class="absolute w-80 h-80 bg-green-300/20 rounded-full blur-3xl top-24 left-20 floating"></div>
  <div class="absolute w-80 h-80 bg-teal-200/20 rounded-full blur-3xl bottom-24 right-20 floating"></div>

  <div data-aos="zoom-in" data-aos-duration="1200">
    <h1 class="text-5xl md:text-6xl font-extrabold mb-6 leading-tight tracking-tight">
      Tempat Mencari Kemakmuran <br>
      <span class="text-lime-300">Dunia dan Akhirat</span>
    </h1>

    <p class="text-lg md:text-xl max-w-2xl mx-auto mb-10 text-gray-100 font-medium"
       data-aos="fade-up"
       data-aos-delay="400">
       Sumber Kemakmuran
    </p>

    <a href="/anggota"
       class="bg-white text-emerald-700 px-8 py-4 rounded-full font-semibold
              transition duration-300 glow hover:scale-105 tracking-wide shadow-md">
       Lihat Anggota
    </a>
  </div>
</section>

<!-- ABOUT SECTION -->
<section id="about" class="py-28 bg-white">
  <div class="container mx-auto text-center px-6">

    <h2 class="text-4xl font-bold text-emerald-800 mb-12 tracking-tight"
        data-aos="fade-right">
        Tentang Kami
    </h2>

    <p class="max-w-3xl mx-auto text-gray-600 leading-relaxed text-lg"
       data-aos="fade-left"
       data-aos-delay="300">
      Kami hadir sebagai wadah pengembangan, penguatan integritas,
      dan semangat kolaboratif untuk menciptakan dampak yang berkelanjutan
      dan bernilai bagi masyarakat.
    </p>

  </div>
</section>

<!-- FOOTER -->
<footer class="bg-gradient-to-r from-emerald-900 to-teal-800 text-white text-center py-10 text-sm tracking-wide">
  © 2026 Sumber Kemakmuran • All Rights Reserved 
</footer>

<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>

<script>
  AOS.init({
    duration: 1200,
    once: true
  });

  // ===== GLOBAL PAGE TRANSITION =====
  document.addEventListener("DOMContentLoaded", function () {
    document.body.classList.add("page-loaded");

    const links = document.querySelectorAll("a");

    links.forEach(link => {
      link.addEventListener("click", function (e) {

        const href = this.getAttribute("href");

        // Ignore anchor links and external links
        if (!href || href.startsWith("#") || this.hostname !== window.location.hostname) {
          return;
        }

        e.preventDefault();

        document.body.classList.remove("page-loaded");
        document.body.classList.add("page-exit");

        setTimeout(() => {
          window.location.href = href;
        }, 400);
      });
    });
  });
</script>

</body>
</html>
